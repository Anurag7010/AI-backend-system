# AI Backend (Python FastAPI)
NEXT_PUBLIC_AI_BACKEND_URL=http://localhost:8000

# PostgreSQL
DATABASE_URL=postgresql://postgres:password@localhost:5432/ai_product

# Auth — generate a strong secret before production
# openssl rand -base64 32
JWT_SECRET=your-secret-here-change-before-production

# OpenAI (used by Python backend — reference here for completeness)
OPENAI_API_KEY=sk-...

# Logging
LOG_LEVEL=info